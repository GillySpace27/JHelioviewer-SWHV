# Synthesis

## Order
1. PR #2 (PUNCH source) FIRST: most self-contained (4 files if refresh dropped: 2 new + 2 one-liners), purely additive, zero overlap with the other two, touches no GL/shader/UBO code, and exactly mirrors the existing SoarClient/SoarDialog pair so Bogdan can pattern-match the diff instantly. Highest acceptance odds per LOC. Land the refresh button (DifferencePanel edit + WeakHashMap machinery) as a SEPARATE follow-up or drop it. The only real blocker is a policy question (PUNCH-specific vs generic registry) that must go to Bogdan up front, but the code risk is the lowest of the three.
2. PR #1 (RHEF + Upsilon) SECOND: medium risk, mostly mechanical decode-chain plumbing (@Nullable Region threaded through ~9 files) plus one genuinely hard line (FitsMetaData sun-shift for nonzero CRVAL) and one shared shader edit (struct Display gains float upsilon, consuming an existing pad float). Do it before the disk PR so the solarCommon.frag touch (Display struct) lands and is verified first; the disk PR touches a DIFFERENT struct in the same file (Screen), so sequencing them avoids a simultaneous two-struct edit. RHEF is also the most scientifically valuable standalone feature and the Algorithm-interface widening question is contained.
3. PR #3 (LogDisk/PowerDisk + flat override) LAST: largest and highest-risk (new MapMode entries, new ProjectedMap Java/GLSL dual math, three new shaders, Screen-UBO field additions, exhaustive-switch edits across MapView/ProjectedMap/GLRenderer, optional ~300-LOC ProjectionOptionsPanel). It depends on nothing from #1 or #2 functionally, but it is the biggest review surface and the most bikesheddable (projection naming, exponent tunability), so land it after the two simpler PRs have built trust. Split internally into 4-5 commits and offer the hardcoded-p / no-panel minimal variant first.

## Shared files
- resources/glsl/solarCommon.frag — touched by PR #1 (append `float upsilon;` to struct Display + an upsilon block in getColor) AND PR #3 (append `float yParam; float diskFlatRadius;` to struct Screen). DIFFERENT structs, non-overlapping line ranges, so the two edits are mechanically independent and BOTH stay applicable off upstream/master. Coordination: whichever lands first leaves the other's region untouched; the second PR rebases on upstream after the first merges and re-confirms its struct still packs (the std140 UBO is the real coupling, not the text). Recommend PR #1's Display-struct edit lands before PR #3's Screen-struct edit so only one struct changes per merge and each can be visually re-verified across all six solar shaders independently.
- src/org/helioviewer/jhv/opengl/GLSLSolarShader.java — PR #1 appends a `float upsilon` param to bindDisplay(...) and `displayBuf.put(upsilon)` (fills float index 22, an existing pad slot; capacity 24 unchanged). PR #3 adds static instances diskLog/diskPower/diskFlat + their _init/_dispose calls and appends two puts to bindScreen (yParam, diskFlatRadius into the existing 28-float screenBuf, 25 written today). Different methods (bindDisplay vs bindScreen) and different static-instance lists, so edits are independent; neither resizes its buffer. Keep them in separate PRs; on rebase, the later PR re-applies its own method edits cleanly.
- src/org/helioviewer/jhv/opengl/GLImage.java — PR #1 adds `private double upsilon = 1;` + setUpsilon/getUpsilon + JSON round-trip + a `, (float) upsilon` arg to the bindDisplay call in applyFilters(). PR #3 adds `private boolean flatInDisk` + isFlatInDisk/setFlatInDisk + JSON round-trip. Adjacent but non-overlapping field/method additions and adjacent JSON opt*/put lines; trivially independent. Both follow the existing `enhanced` field as the one-for-one template.
- src/org/helioviewer/jhv/layers/filters/ImageFilterPanel.java — PR #1 adds the Upsilon JideSplitButton(Υ) to buttonPanel LINE_START (RHEF radio auto-appears from the enum loop, no radio edit). PR #3 adds the flat-in-disk JideToggleButton(◯) also to buttonPanel LINE_START. Both target the same buttonPanel sub-region, so this is the one file where the two PRs genuinely collide on layout. Coordination: land #1 first; #3 rebases and adds its toggle alongside (decide LINE_START ordering then). If both buttons crowd LINE_START, the second PR should cluster them in a small JPanel(FlowLayout LEADING) rather than fight over the constraint slot.

## Per-PR

### PR #2 — Add PUNCH archive source (SDAC)
files: src/org/helioviewer/jhv/io/PunchClient.java [NEW] — port punch-source:io/PunchClient.java verbatim in logic; mechanical renames only: org.helioviewer.jhv.app.Log (not top-level Log), org.helioviewer.jhv.thread.Task.submit (not threads.Tasks; 4-arg shape logContext/Callable/Consumer/errorMessage confirmed Task.java:24), org.helioviewer.jhv.app.Commands.loadImage (confirmed Commands.java:158), org.helioviewer.jhv.layers.ImageLayer, org.helioviewer.jhv.time.TimeUtils.DAY_IN_MILLIS. PRESERVE: BASE_URL=https://umbra.nascom.nasa.gov/punch; FILE_PATTERN/DIR_PATTERN/NUM_DIR_PATTERN regexes; record DataItem(String file, URI uri, long milli); the cadence-overflow-safe seed `long lastKept = start - Math.max(1, cadence) - 1;` (commit 36af4cce6 fix — non-negotiable); NetClient.of(uri, true, NetClient.NetCache.NETWORK) + nc.isSuccessful()/nc.getSource().readUtf8(); QueryItems/QueryProducts/QueryCoverage as private records implementing Callable. `public final class PunchClient` with private ctor at bottom (mirror SoarClient.java:18,155)., src/org/helioviewer/jhv/gui/dialog/PunchDialog.java [NEW] — port punch-source:gui/dialogs/PunchDialog.java to gui.dialog (singular). Renames: MainFrame.get() (not JHVFrame.getFrame() — confirmed MainFrame.java:257), org.helioviewer.jhv.gui.time.TimeSelectorPanel (confirmed; getStartTime/getEndTime/setTime/addListener all present), org.helioviewer.jhv.app.Message (err/warn), org.helioviewer.jhv.movie.Player.getStartTime/getEndTime (confirmed Player.java:104,108), TimeUtils.START.milli sentinel + TimeUtils.format. Keep StandardDialog structure, MAX_FILES=5000/CONFIRM_FILES=200, Dimension(500,350), Level={3,Q,2,1,0}, ProductInfo tooltip map, Cadences presets, default product PTM, coverage-probe + movie-prefill empty-state UX, three setPunchResponseXxx callbacks. Title 'New PUNCH Layer'., src/org/helioviewer/jhv/gui/Actions.java [EDIT] — add `import org.helioviewer.jhv.gui.dialog.PunchDialog;` near Actions.java:25; add `public static class NewPunchLayer extends AbstractAction { public NewPunchLayer() { super("New PUNCH Layer..."); } @Override public void actionPerformed(ActionEvent e) { PunchDialog.getInstance().showDialog(); } }` after NewSynopticLayer (~:117). No accelerator (Ctrl+Shift+N taken by SOAR)., src/org/helioviewer/jhv/gui/component/MenuBar.java [EDIT] — one line: `fileMenu.add(new Actions.NewPunchLayer());` after `fileMenu.add(new Actions.NewSynopticLayer());` at MenuBar.java:29, before OpenLocalFile., src/org/helioviewer/jhv/layers/filters/DifferencePanel.java [EDIT — OPTIONAL, RECOMMEND DROP/DEFER] — only if shipping per-layer refresh; adds an io->layers.filters dependency and the WeakHashMap/QueryState/RefreshResult machinery to PunchClient. Strongly recommend splitting to a follow-up PR to keep #2 to 4 files, pure-additive.
- top style rule: Mirror the SoarClient/SoarDialog pair shape EXACTLY — `public final class` + private no-arg ctor at file bottom; off-EDT via Task.submit("context", new QueryXxx(...), receiver::setter, "Error ..."); query bodies as private `record QueryXxx(...) implements Callable<...>`; nested Receiver interfaces; singleton dialog with getInstance() + private ctor(JFrame), @SuppressWarnings("serial"), extends StandardDialog, overrides createButtonPanel/createContentPanel/createBannerPanel(null), showDialog() does pack/setLocationRelativeTo(MainFrame.get())/setVisible(true). The smaller and more SOAR-shaped the diff, the higher the merge odds.
- rejection risk: POLICY: Bogdan merges only his own work and may decline to host a NASA-mission-specific source hardcoded to one URL (umbra.nascom.nasa.gov/punch). This is a scope decision, not a code-quality one — ASK FIRST (see bogdan_questions). Secondary rejection trigger: bundling the refresh button (DifferencePanel edit) which crosses the IO->UI boundary and bloats the diff; ship #2 as a clean 4-file additive pair and let the refresh land separately.

### PR #1 — RHEF filter + Upsilon midtone control
files: src/org/helioviewer/jhv/image/FilterRHEF.java [NEW] — package-private `class FilterRHEF implements ImageFilter.Algorithm`. Imports: java.util.Arrays; javax.annotation.Nullable; org.helioviewer.jhv.metadata.Region; org.helioviewer.jhv.thread.ParallelRange. `private static final int MIN_BIN_COUNT = 5;`. Two methods: the no-arg `filter(data,w,h)` delegating to `filter(data,w,h,null)`, and `filter(data,w,h,@Nullable Region region)` with the VERBATIM kernel (geometry block from Region; per-pixel annulus binning; counting sort offset[]/order[]; per-bin ParallelRange.run sort-and-rank with packed-long `(long)Float.floatToRawIntBits(v)<<32|idx`, average-rank over equal runs, v<=0 excluded so occulter/pad stays black). Guard `if (width < 1 || height < 1) return data;` at top., src/org/helioviewer/jhv/image/ImageFilter.java [EDIT] — add imports Nullable + Region; add enum entry `RHEF("Radial histogram equalization", new FilterRHEF())` after WOW (CONFIRMED enum is None/MGN/WOW, dispatch is `type == Type.None ? data : filter(...)`); add default interface method `default float[] filter(float[] data, int width, int height, @Nullable Region region) { return filter(data, width, height); }` (MGN/WOW UNCHANGED); thread @Nullable Region through the two private workers and two static entry points (filter(byte[]) + filterHalfFloat(short[]) — both CONFIRMED present, tail of file)., src/org/helioviewer/jhv/image/ImageBuffer.java [EDIT] — CONFIRMED layout: fromBytes 4-arg delegates to 5-arg with Type.None (:32-36); fromShorts (:43); createWriteBuffer (:50) -> WriteBuffer(:99) stores filterType, applies in finish() (:131-136). Add @Nullable Region overloads (Region field on WriteBuffer, `_region` ctor param, forwarded in finish()); RGBA32 path still skips filter (:39, :140)., src/org/helioviewer/jhv/metadata/MetaData.java [EDIT] — add `@Nonnull Region roiToSunRegion(int,int,int,int,double,double);` after roiToRegion (:43)., src/org/helioviewer/jhv/metadata/CommonMetaData.java [EDIT] — CONFIRMED `class CommonMetaData implements MetaData` (:11), roiToRegion at :121-123. Add `protected double sunShiftX = 0; protected double sunShiftY = 0;` near unitPerPixelX/Y (:21-22); implement roiToSunRegion ONCE (Null/Basic/Fits inherit): `Region r = roiToRegion(...); if (sunShiftX==0 && sunShiftY==0) return r; return new Region(r.llx - sunShiftX, r.lly + sunShiftY, r.width, r.height);`., src/org/helioviewer/jhv/metadata/FitsMetaData.java [EDIT — HIGHEST-RISK LINE] — CONFIRMED crval is Vec2 scaled by unitPerArcsec for non-surface maps (:289-291), crota is Quat.createAxisZ(crotaRad) (:297), isSurfaceMap is a LOCAL bool in the WCS block (:266), referenceX/Y instance fields (:284-285). Inside that same block (after crota assigned) add `import org.helioviewer.jhv.math.Vec3;` and: `if (!isSurfaceMap && (crval.x != 0 || crval.y != 0)) { Vec3 sun = crota.rotateInverseVector(new Vec3(-crval.x, -crval.y, 0)); sunShiftX = sun.x; sunShiftY = sun.y; }`. The Y sign (`r.lly + sunShiftY`) inherits the POC pixel-flip convention and MUST be validated on a real off-center image — flag to Bogdan., src/org/helioviewer/jhv/view/j2k/J2KDecoder.java [EDIT] — CONFIRMED record header (:27) and createWriteBuffer call (:104). Add @Nullable Region component and pass to createWriteBuffer., src/org/helioviewer/jhv/view/j2k/J2KView.java [EDIT] — add a `filterRegion(decodeParams)` helper using metaData[frame].roiToSunRegion(roi.x/y/w/h, resolution.factorX/Y) and pass it to the new J2KDecoder; LEAVE the post-decode display Region (sendDataToHandler m.roiToRegion) UNCHANGED., src/org/helioviewer/jhv/view/uri/URIImageReader.java [EDIT] — CONFIRMED interface already imports @Nullable; method is `ImageBuffer readImageBuffer(File file, ImageFilter.Type filterType)`. Add @Nullable Region param., src/org/helioviewer/jhv/view/uri/URIView.java [EDIT] — CONFIRMED imageRegion (:33,:57), Decoder record (:93), readImageBuffer call (:97). Add `private final Region filterRegion = m.roiToSunRegion(0,0,buffer.width,buffer.height,1,1);`, add Region component to Decoder record, pass through to reader., src/org/helioviewer/jhv/view/uri/FITSImage.java [EDIT] — thread @Nullable Region through readImageBuffer/readHDU/readPixels to all 3 createWriteBuffer calls., src/org/helioviewer/jhv/view/uri/GenericImage.java [EDIT] — thread @Nullable Region through readImageBuffer/readBuffered to fromBytes(Gray8)/fromShorts(Gray16F); RGBA32 path unchanged., resources/glsl/solarCommon.frag [EDIT — SHARED w/ PR#3] — CONFIRMED struct Display ends `vec2 slit;` (:74) with no upsilon; getColor ends `value += dither(texcoord); return texture(...)` (:156-158). Append `float upsilon;` AFTER `vec2 slit;` (consumes an existing trailing pad float, no UBO resize); insert the verbatim double-sided-gamma upsilon block after the sharpen mix and BEFORE `value += dither`., src/org/helioviewer/jhv/opengl/GLSLSolarShader.java [EDIT — SHARED w/ PR#3] — CONFIRMED displayBuf=24 floats, bindDisplay writes 22 (:155-167). Append `float upsilon` param + `displayBuf.put(upsilon)` (fills float index 22). Capacity UNCHANGED., src/org/helioviewer/jhv/opengl/GLImage.java [EDIT — SHARED w/ PR#3] — add `private double upsilon = 1;` after enhanced; setUpsilon(double _upsilon){upsilon=Math.clamp(_upsilon,0.05,1);} + getter; append `,(float)upsilon` to the bindDisplay call; JSON round-trip mirroring enhanced., src/org/helioviewer/jhv/layers/filters/ImageFilterPanel.java [EDIT — SHARED w/ PR#3] — KEEP radio modePanel (RHEF auto-appears from the Type.values() loop); add only the Upsilon JHVSlider(5,100) + JLabel + JideSplitButton("Υ") to buttonPanel LINE_START; listener: setUpsilon(value) then DisplayController.display() (render-time, NO clearCache). Filter-type change keeps clearCache()+render(1).
- top style rule: Render-vs-decode refresh discipline (the whole point of the hybrid): filter-type change => layer.getView().clearCache(); setFilter(type); DisplayController.render(1) (forces re-decode). Upsilon slider => DisplayController.display() ONLY (GPU uniform, no re-decode). Plus: package-private `class FilterRHEF` mirroring FilterMGN; ParallelRange.run for every hot loop; underscore ctor/setter params (_upsilon, _region); @Nullable from javax.annotation; Math.clamp (Java 25); upsilon field LAST in struct Display so existing std140 offsets are untouched.
- rejection risk: The ImageFilter.Algorithm contract widening: adding `filter(data,w,h,@Nullable Region)` to a previously metadata-free interface, plus threading @Nullable Region through ~9 decode-chain files, is a broad diff for one filter. If Bogdan rejects widening the interface, the fallback is a separate metadata-aware Algorithm variant — confirm BEFORE writing. Second risk: the FitsMetaData sun-shift Y-sign on off-center CRVAL; if wrong the annuli center off-Sun and equalization smears (only affects PUNCH/shifted-LASCO; CRVAL==0 imagery is a no-op). Validate against a real off-center frame and quarantine that edit as its own commit.

### PR #3 — LogDisk + PowerDisk projections + flat-in-disk override
files: resources/glsl/solarCommon.frag [EDIT — SHARED w/ PR#1, UNAVOIDABLE] — CONFIRMED struct Screen ends `float yStart; float yStop;` (:56-57); screenBuf is 28 floats with 25 written. Append `float yParam; float diskFlatRadius;` to struct Screen (no UBO resize). DIFFERENT struct from PR#1's Display edit — independent., resources/glsl/solarDiskLog.frag [NEW] — template solarPolar.frag; add private getDiskPos() (raw up1.xy, no clamp/aspect); main(): w=getDiskPos(); t=2*length(w); discard t>1||t==0; angle=atan(-w.x,w.y) wrapped; clamp_coord(vec2(angle/TWOPI,t)); radialCoordinate=exp(yStart+t*(yStop-yStart)); sampleDiskTexcoord with polarXY=(2*radialCoordinate/t)*vec2(w.x,-w.y), verbatim cutOff block, rotate_plane_inverse/rect/clamp_texture., resources/glsl/solarDiskPower.frag [NEW] — identical to solarDiskLog.frag except radialCoordinate=pow(yStart+t*(yStop-yStart), screen.yParam)., resources/glsl/solarDiskFlat.frag [NEW] — flat/limb-pegged override; discard if screen.diskFlatRadius<=0; polarXY=2*screen.diskFlatRadius*vec2(w.x,-w.y); radialCoordinate=length(polarXY); enhancementFactor=1., src/org/helioviewer/jhv/math/PolarBasis.java [EDIT — UNAVOIDABLE] — CONFIRMED `angle(Vec3 v)` exists (:13) but no double-overload. Add `public static double angle(double x, double y){ double t=Math.atan2(-x,y); if(t<0) t+=2*Math.PI; return t; }` and refactor angle(Vec3) to delegate., src/org/helioviewer/jhv/display/MapMode.java [EDIT — UNAVOIDABLE] — CONFIRMED enum at display/MapMode.java with `MapMode(GLSLSolarShader _shader, Kind _kind)` and `enum Kind { ORTHOGRAPHIC, HPC, LATITUDINAL, POLAR }`. Add `LogDisk(GLSLSolarShader.diskLog, Kind.DISK), PowerDisk(GLSLSolarShader.diskPower, Kind.DISK)` after Polar; add DISK to inner Kind., src/org/helioviewer/jhv/display/MapView.java [EDIT — UNAVOIDABLE] — CONFIRMED create() branches ORTHOGRAPHIC vs ProjectedView (:31), projectedKind exhaustive switch (:165). Add `public boolean isDisk(){ return mode.kind == MapMode.Kind.DISK; }` (matches existing isPolar/isHpc predicates used by PositionStatusPanel); add `case DISK -> ProjectedMap.Kind.DISK;` to projectedKind; thread the new Kind param into the mouseToGrid/mouseToScreen ProjectedView calls., src/org/helioviewer/jhv/display/ProjectedMap.java [EDIT — UNAVOIDABLE] — CONFIRMED `final class ProjectedMap`, `enum Kind {HPC,LATITUDINAL,POLAR}` (:17), project()/unproject() exhaustive switches, projectPolar (:49). NOTE: mouseToGrid currently has NO Kind param (signature mouseToGrid(camera,width,vp,scale,gridType,x,y)) — adding `Kind kind` first param is a real change touching MapView call sites too. Add DISK to Kind; project DISK->projectDisk; unproject POLAR,DISK->unprojectPolar; projectToScreen/emitMapLine/emitMapPoints emit raw pt for DISK (no *vp.aspect, no horizontal wrap); mouseToGrid DISK->mouseToDiskGrid. Add private projectDisk + mouseToDiskGrid (verbatim from POC)., src/org/helioviewer/jhv/display/MapScale.java [EDIT — UNAVOIDABLE] — CONFIRMED `interface MapScale`, MapScaleBase ctor does `yStart = scaleY(_yStart)` at :50 BEFORE subclass fields init (virtual-call constraint REAL), LogMapScale at :105. Add `default double getYParam(){ return 1; }`; add diskLog/diskPower factories; add `final class PowerMapScale extends MapScaleBase` with a STATIC `power()` accessor (NOT an instance field — would read 0.0 during super()) overriding scaleY=pow(val,power())/invScaleY/getYParam=1/power()., src/org/helioviewer/jhv/display/Display.java [EDIT — OPTIONAL] — only if exponent/range tunable: diskPower (default 0.5, clamp[-2,2], dead-zone snap |p|<0.05->±0.05), logDiskRMin(0.9), rMax(0=auto) + setters. Drops entirely if PowerMapScale hardcodes p=0.5 and the panel is dropped., src/org/helioviewer/jhv/opengl/GLSLSolarShader.java [EDIT — SHARED w/ PR#1, UNAVOIDABLE] — CONFIRMED static instances list + init()/dispose() list every shader. Add diskLog/diskPower/diskFlat instances + their _init/_dispose (in BOTH lists); in bindScreen append `.put((float)scale.getYParam())` and `.put(t1>1e-4?(float)(1/t1):0)` where `double t1=scale.getYValueInv(1)+.5`. screenBuf alloc UNCHANGED (28 floats)., src/org/helioviewer/jhv/opengl/GLRenderer.java [EDIT — UNAVOIDABLE, exhaustive switch] — CONFIRMED createScales (:32) with Polar/LogPolar cases calling MapScale.{polar,logpolar}(ImageLayers.getLargestRadialSize()) (:37-38, getLargestRadialSize CONFIRMED ImageLayers.java:52). Add LogDisk/PowerDisk cases via createConstantScales + MapScale.diskLog/diskPower., src/org/helioviewer/jhv/opengl/GLImage.java [EDIT — SHARED w/ PR#1] — add `private boolean flatInDisk = false;` + isFlatInDisk/setFlatInDisk + JSON round-trip (key 'flatInDisk' alongside 'enhanced')., src/org/helioviewer/jhv/layers/ImageLayer.java [EDIT — UNAVOIDABLE for flat override] — CONFIRMED renderScale (:195) line 212 `GLSLSolarShader shader = mv.mode().shader;`. Change to `GLSLSolarShader shader = mv.isDisk() && glImage.isFlatInDisk() ? GLSLSolarShader.diskFlat : mv.mode().shader;`., src/org/helioviewer/jhv/layers/grid/DiskGrid.java [NEW] — port verbatim (uses modern grid/text API): chooseRings (1-2-5 decade, MIN_RING_SPACING=0.04, MAX_RINGS=16) at ringRho=.5*(getYValueInv(r)+.5), 180-seg GLSLLine circles + 30deg spokes, Colors.Null bracketing, labels via SdfTextRenderer/GLText.renderer()/FastFormat.rounded2 + FlatGrid sizing idiom., src/org/helioviewer/jhv/layers/GridLayer.java [EDIT] — add `private final DiskGrid diskGrid = new DiskGrid();`; in renderScale `if (mv.isDisk()) diskGrid.render(mv,vp,showLabels); else flatGrid.render(...)`; diskGrid.init()/dispose() in init()/dispose()., src/org/helioviewer/jhv/gui/status/PositionStatusPanel.java [EDIT — UNAVOIDABLE for status] — CONFIRMED `if(isHpc) ... else if(isLatitudinal) ... else if(isPolar()||isLogPolar()) setText(formatPolar)` (:43-48). Change the polar branch to `|| mv.isDisk()` so disk reuses formatPolar (PA deg, R_sun)., src/org/helioviewer/jhv/layers/filters/ImageFilterPanel.java [EDIT — SHARED w/ PR#1] — add the flat-in-disk JideToggleButton("◯", isFlatInDisk()) to buttonPanel LINE_START; listener setFlatInDisk then DisplayController.display(). Coordinate LINE_START layout with PR#1's Upsilon button., src/org/helioviewer/jhv/gui/component/ProjectionOptionsPanel.java [NEW — OPTIONAL, own commit] — exponent JHVSlider + JHVRangeSlider (NOT raw jidesoft RangeSlider) + grid-color JButton; MUST `implements ViewState.ModeListener` (functional, modeStateChanged()) NOT the POC's Runnable method-ref. gui.component package., src/org/helioviewer/jhv/gui/JHVFrame.java [EDIT — OPTIONAL, panel commit] — add ProjectionOptionsPanel to left pane + ViewState.addModeListener(panel)., src/org/helioviewer/jhv/layers/Layers.java [EDIT — OPTIONAL] — getGridLayer() accessor, only if the grid-color picker ships.
- top style rule: Never reorder existing std140 UBO fields — only APPEND (yParam, diskFlatRadius after yStop); keep enum switches EXHAUSTIVE with no default and the ORTHOGRAPHIC-style throw for impossible cases; PowerMapScale's exponent MUST come from a static accessor (the MapScaleBase ctor virtually calls scaleY() before subclass fields init — a `private final double power` field reads 0.0 during super() and breaks the scale); re-point every POC import (gui.component not gui.components.base, gui.status not gui.components.statusplugin, metadata.Region, JHVRangeSlider wrapper, ViewState.ModeListener functional interface).
- rejection risk: Size and bikeshedding. The shared solarCommon.frag Screen-struct edit is the single highest-risk line: appending two floats stays within the 28-float alloc but the ANGLE/LWJGL driver is the final std140 arbiter — EVERY one of the six existing solar modes (sphere/ortho/hpc/lati/polar/logpolar) must be re-verified to render identically. Combined with the optional ~300-LOC ProjectionOptionsPanel + JHVFrame/Layers wiring, a maximal single PR is likely rejected. Mitigation: split into 4-5 commits (UBO+shaders+instances; Java projection+PolarBasis+GLRenderer; DiskGrid+GridLayer+status; flat override; OPTIONAL panel) and offer the hardcoded-p / no-panel minimal variant first.

## Bogdan questions
- [PR#2 — POLICY, BLOCKING, ASK FIRST] Do you want JHelioviewer to host a PUNCH-specific remote source (a NewPunchLayer action + PunchDialog hardcoded to umbra.nascom.nasa.gov/punch, scraping the Apache directory index since there is no TAP/ADQL API as SOAR has), or would you prefer a GENERIC remote-archive abstraction (a small registry of {name, baseUrl, file-regex, layout-template}) of which PUNCH is one configured instance? The POC is PUNCH-specific and the smaller diff; a generic design is more reusable but speculative. This gates the entire shape of PR#2 — answer before any code is written.
- [PR#1 — CONTRACT] Is it acceptable to widen the ImageFilter.Algorithm interface from the stateless `float[] filter(float[] data,int w,int h)` to add `default float[] filter(float[] data,int w,int h,@Nullable Region region)` (MGN/WOW inherit the no-op), and to thread @Nullable Region through the decode chain (J2KDecoder/J2KView, URIImageReader/URIView/FITSImage/GenericImage, ImageBuffer)? RHEF needs Sun-center + pixel scale for its radial annulus binning and Region already carries that. If you'd rather not touch the interface, do you prefer a separate metadata-aware Algorithm variant?
- [PR#1 — HIGHEST-RISK MATH] For nonzero-CRVAL (off-center Sun: PUNCH, shifted LASCO), I plan to compute a sun-shift on FitsMetaData from crval de-rotated by crota — using the current fields crval (Vec2, already *unitPerArcsec for non-surface maps, FitsMetaData.java:289-291) and crota (Quat, :297): `Vec3 sun = crota.rotateInverseVector(new Vec3(-crval.x,-crval.y,0))` then roiToSunRegion returns `new Region(r.llx - sunShiftX, r.lly + sunShiftY, ...)`. Are crval and referenceX in the same linear units here, and is the Y sign correct given `referenceY = unitPerPixelY*(pixelH - crpix2)` (pixel-row flip)? This is the one spot the pre-refactor POC math must be re-derived on current upstream.
- [PR#3 — TUNABILITY] PowerDisk exponent p: ship it tunable (Display.getDiskPower + slider, range [-2,2] with a dead-zone snap near 0) or fix it at p=0.5 to minimize the diff (drops Display.java statics, the panel, and the slider)? The v1 design fixed 0.5; the POC later extended to [-2,2].
- [PR#3 — SCOPE] Do you want the optional ProjectionOptionsPanel (left-pane exponent/radial-range/grid-color controls, ~300 LOC + JHVFrame + Layers wiring) in this PR, or should disk modes ship with sensible defaults only and the panel land later? It is the largest, most-bikesheddable chunk.
- [PR#3 — UBO] Is appending two floats (yParam, diskFlatRadius) to the shared Screen std140 block (used by all six solar shaders) acceptable, or would you prefer the disk-only floats packed into a separate small UBO to avoid touching the struct every solar shader includes? Same question for PR#1's `float upsilon` on the Display block.
- [PR#3 — ARCH] After #314 (unify non-ortho coordinate handling) / #316 (surface map WCS projections), is a new Sun-centered disk remap best expressed as a new MapMode enum entry + new GLSLSolarShader instance (mirroring Polar/LogPolar), or do the surface-map WCS projections now subsume custom radial remaps so it should be a WcsHeader.Projection instead?
- [PR#3 — ARCH] Is per-layer MapMode override still supported after #314, and is the layer filter buttonPanel the right home for the flat-in-disk toggle (◯), or should map mode be strictly global display state with the override hooked elsewhere?
- [PR#1 — NAMING/SCOPE] Enum description 'Radial histogram equalization' / toString 'RHEF' OK, or do you want the fuller name / a Gilly & DeForest citation in the tooltip? And should the GPU upsilon curve be gated to filterType==RHEF or always-on (identity at upsilon=1)? Confirm the [0.05,1] softening-only clamp.
- [PR#2 — MINOR] Confirm menu placement/label: 'New PUNCH Layer...' in the File menu right after 'New Synoptic Layer...', no accelerator (Ctrl/Cmd+Shift+N taken by SOAR). And acceptable load bounds: MAX_FILES=5000 with a 200-file confirmation prompt, or a hard cap / max time-span? Should the per-layer refresh button ship here (touching DifferencePanel), be a follow-up PR, or be dropped?
- [CROSS-CUT — RENDER, not blocking PR#2 data access] Will PUNCH L2/L3 large-FOV (~45deg) helioprojective WCS render with correct on-sphere placement/scale after #314/#316? Frames load through the existing FITS path regardless; this is about correct placement and is the implicit value link between PR#2 (gets the data in) and PR#3 (views it well).

## Verification
Spot-checked 14 load-bearing claims against upstream/master (head 525e4c59a, confirmed). VERIFIED TRUE: (1) ImageFilter.Type is None/MGN/WOW with `type==Type.None ? data : filter(...)` dispatch, Algorithm is single-method `float[] filter(float[],int,int)`, both static entry points present. (2) GLSLSolarShader has 6 static solar instances (sphere/ortho/hpc/lati/polar/logpolar), displayBuf=24 floats with bindDisplay writing 22 (2 pad slots — upsilon claim holds), screenBuf=28 floats writing 25 (3 pad — yParam/diskFlatRadius claim holds), init()/dispose() list every instance. (3) solarCommon.frag struct Display ends `vec2 slit;` (line 74), struct Screen ends `float yStart; float yStop;` (lines 56-57), getColor ends with `value += dither; return texture(...)` (insertion point confirmed). (4) MapMode.java is display/MapMode.java with `MapMode(GLSLSolarShader _shader, Kind _kind)` + Kind{ORTHOGRAPHIC,HPC,LATITUDINAL,POLAR}. (5) MapView.create branches ORTHOGRAPHIC (line 31); projectedKind exhaustive switch throws on ORTHOGRAPHIC (line 165). (6) MapScaleBase ctor does `yStart = scaleY(_yStart)` at line 50 BEFORE subclass init — the PowerMapScale static-power() constraint is REAL. (7) J2KDecoder record (line 27) + createWriteBuffer (line 104), ImageBuffer fromBytes/fromShorts/createWriteBuffer/WriteBuffer all as described. (8) FitsMetaData crval=Vec2 (*unitPerArcsec non-surface, :289-291), crota=Quat (:297), referenceX/Y (:284-285), roiToRegion override (:334). (9) SoarClient `public final class`+private ctor, Task.submit 4-arg (Task.java:24), QueryXxx private records, Receiver interfaces. (10) Actions NewSoarLayer/NewSynopticLayer pattern + gui.dialog import; MenuBar file menu order; Commands.loadImage(List<URI>) returns CompletableFuture<ImageLayer> (:158). (11) MainFrame.get() (:257), TimeSelectorPanel at gui.time with getStartTime/getEndTime/setTime/addListener, Player.getStartTime/getEndTime (movie.Player :104,108). (12) ImageLayer.renderScale line 212 `GLSLSolarShader shader = mv.mode().shader;`. (13) GLRenderer.createScales exhaustive switch (:32) with Polar/LogPolar via MapScale.{polar,logpolar}(ImageLayers.getLargestRadialSize()); getLargestRadialSize confirmed. (14) PolarBasis.angle(Vec3) exists (:13) but angle(double,double) does NOT — disk refactor needed; PositionStatusPanel polar branch confirmed (:43-48). CORRECTIONS TO THE SPECS: (a) ProjectedMap.mouseToGrid currently has NO Kind param (signature is camera,width,vp,scale,gridType,x,y) — the disk plan's 'add Kind param' is a real signature change touching MapView call sites, slightly larger than the spec framed it. (b) URIImageReader already imports @Nullable but readImageBuffer has no Region param — additive edit is correct. (c) FitsMetaData isSurfaceMap is a LOCAL bool inside the WCS block, not a field — the sunShift edit must live in that same block (it does). All three PRs' load-bearing paths and signatures hold against current upstream; no plan claim was found false, only the two scope clarifications above.